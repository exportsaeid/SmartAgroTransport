<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5"
    android:padding="16dp">

    <!-- هدر -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="24dp">

        <ImageView
            android:id="@+id/backButtonProfit"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="برگشت"
            app:tint="#424242" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="گزارش سود و ضرر"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#1976D2"
            android:gravity="center" />

    </LinearLayout>

    <!-- جمع کل سود/ضرر -->
    <TextView
        android:id="@+id/tvTotalProfit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="جمع کل سود/ضرر: در حال محاسبه..."
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#424242"
        android:gravity="center"
        android:background="#FFFFFF"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        android:elevation="4dp" />
    <!-- بعد از هدر و قبل از tvTotalProfit -->
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchViewProfit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:queryHint="جستجو بر اساس شماره فاکتور یا نام خریدار..."
        android:layout_marginBottom="16dp" />
    <!-- پیام وقتی لیست خالی است -->
    <TextView
        android:id="@+id/tvEmptyProfit"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="هیچ فاکتور تکمیل شده‌ای (تحویل به خریدار) وجود ندارد."
        android:textSize="18sp"
        android:textColor="#757575"
        android:gravity="center"
        android:visibility="gone" />

    <!-- لیست گزارش -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerProfit"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingBottom="8dp" />

</LinearLayout>